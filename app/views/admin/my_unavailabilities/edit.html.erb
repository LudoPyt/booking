<div class="corps">

    <h1>Modifier une indisponibilité</h1>
      <%= simple_form_for [:admin,@meet] do |f| %>

        <%= f.input :reason %>
        <%= f.input :date%>
        <%= f.button :submit, "Modifier votre indisponibilité", class: "btn btn-danger mt-4 mb-3 w-30 text-center", id: "cancel" %>

      <% end %>

</div>
